# Use a Python base image
FROM python:3.8

# Set a sensitive environment variable (for demonstration purposes)
ENV PASSWORD=mysecretpassword

# Specify a command to run your application
CMD ["python", "app.py"]
 
